# Database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/puddle

# MCP Server
MCP_SERVER_URL=http://localhost:8002/puddle-mcp/mcp

# LLM Provider - Choose ONE of these setups:

## Setup 1: Separate OpenRouter keys for ACID and TIDE (RECOMMENDED)
# Allows using different models/accounts for each agent
OPEN_ROUTER_KEY_ACID=sk-or-v1-...
OPEN_ROUTER_KEY_TIDE=sk-or-v1-...
# Optional: Override models per agent
LLM_MODEL_ACID=anthropic/claude-3.5-sonnet
LLM_MODEL_TIDE=google/gemini-2.0-flash-exp

## Setup 2: Shared OpenRouter key (both agents use same key)
# OPENROUTER_API_KEY=sk-or-v1-...

## Setup 3: OpenAI (fallback if no OpenRouter keys found)
# OPENAI_API_KEY=sk-...

# Optional: Override default model for both agents (if not using agent-specific)
# Defaults to: anthropic/claude-3.5-sonnet (OpenRouter) or gpt-4o (OpenAI)
# LLM_MODEL=anthropic/claude-3.5-sonnet

# JWT Secret
SECRET_KEY=your-secret-key-here
